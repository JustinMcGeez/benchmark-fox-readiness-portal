# Build Queue — Benchmark Fox Readiness Portal

Rules: strictly in order. One task per Claude Code session (/clear between).
Merge each task's branch before starting the next. [security] tasks get the
security-reviewer subagent in addition to verifier.

- [x] 01 — Testing infrastructure (Vitest + Playwright + CI) — prompts/01-testing-infrastructure.md
- [x] 02 — React Router, client-scoped URLs — prompts/02-react-router.md
- [x] 03 — Supabase Auth [security] — prompts/03-supabase-auth.md
- [x] 04 — Repository layer: localStorage → Supabase [security] — prompts/04-repository-layer.md
- [x] 05 — RLS hardening + automated policy tests [security] — prompts/05-rls-hardening.md
- [x] 06 — Real audit logging — prompts/06-audit-logging.md
- [x] 07 — Client CRUD + assignments — prompts/07-client-crud.md
- [x] 08 — Evidence lifecycle workflow — prompts/08-evidence-workflow.md
- [x] 09 — SSP generator (.docx) — prompts/09-ssp-docx.md
- [x] 10 — POA&M (.xlsx) + SPRS report (.pdf) — prompts/10-poam-xlsx-sprs-pdf.md
- [x] 11 — Client portal (role-scoped) [security] — prompts/11-client-portal.md
- [ ] 12 — Resilience pass (errors, Sentry, a11y) — prompts/12-resilience-pass.md
- [ ] 13 — CI/CD hardening + security headers — prompts/13-cicd-hardening.md
- [ ] 14 — Staff-only consultant-notes table [security] — prompts/14-consultant-notes-table.md

## Completed log
(/build-next appends here: date, task, one-line summary)

- 2026-06-12 — 01 Testing infrastructure: Vitest (63 unit tests: scoring/selectors/objectives/store/integrity, src/lib at 100% line coverage for tested modules) + Playwright chromium smoke suite (5 tests) + CI workflow; verifier PASS.
- 2026-06-12 — 02 React Router: replaced the custom ?screen= router with react-router-dom@6 — client-scoped URLs for all 21 screens (src/routes.tsx: route tree, ScreenKey↔path map, legacy ?screen= redirect, ProtectedRoute placeholder, NotFound); keyboard/launcher/tweaks UX preserved; +9 unit and +3 e2e tests; verifier PASS.
- 2026-06-12 — 03 Supabase Auth: migration 002 (profiles.role default readonly_viewer, invite-safe handle_new_user/handle_user_email_confirmed triggers, self-scoped SELECT-only RLS policy) + authService/AuthProvider (generic errors, magic-link anti-enumeration), real Login (password + magic link, return-to-destination), ProtectedRoute/RequireRole, Shell user menu; Local Prototype mode keeps working behind a dismissible banner; +7 unit and +2 e2e tests (1 real-auth stub skipped); verifier PASS, security-reviewer PASS (after invite-capture hardening). NOTE for Supabase project setup: disable public email signups (invite-only portal).
- 2026-06-12 — 05 RLS hardening [security]: migration 004_rls_policies.sql turns the draft into the applied tenancy boundary — 6 SECURITY DEFINER helpers (repointed from the empty user_roles table to the authoritative profiles.role; STABLE, search_path=public, execute granted to authenticated only) + policies on all 18 RLS-enabled tables with a deny-all checklist. admin r/w all; consultant r/w only assigned clients; client_executive/it_owner/readonly_viewer read-only on their one assigned client; evidence_uploader reads assigned client + insert/update evidence metadata only (cannot write assessments); reference tables read-only via auth keys (service_role-only writes); audit_events append-only (insert on accessible clients, no update/delete). Every write carries WITH CHECK; no tenant policy uses auth.uid() directly. Kept 002's no-self-update on profiles (added admin-read only) — no self role-escalation. Automated suite scripts/test-rls.mjs (node:test + @supabase/supabase-js, no new deps) proves cross-client reads return zero rows, writes are blocked, reference is read-only, audit is immutable; new CI `rls` job (supabase/setup-cli + supabase start) enforces it. Gate green (typecheck/build:data/build/127 unit tests/check:supabase-readonly); RLS suite not run locally (no Docker/Supabase CLI on dev machine) — CI is the enforcement point. verifier PASS, security-reviewer PASS (no exploitable cross-client path; column-level hiding of internal notes from client-role users deferred to Task 11 portal — row-level isolation is complete and no client-role users exist pre-portal). NEEDS HUMAN: apply migration 004 to the Supabase project; confirm the `rls` CI job goes green on first push (validates config.toml against the CLI version).
- 2026-06-12 — 06 Real audit logging: migration 005_audit_triggers.sql makes the Audit Log real — AFTER INSERT/UPDATE triggers on assessments/intake/scope/evidence write a compact {field:{old,new}} jsonb diff into audit_events.new_value (changed columns only; id/timestamps/FK columns excluded; free-text >500 chars collapsed to '[text changed]' so no CUI-adjacent text enters the trail; empty diffs skipped → exactly one row per meaningful edit; actions <entity>.created/.updated, intake/scope .saved, evidence.status_changed). A BEFORE INSERT trigger (audit_set_actor) stamps user_id + the new denormalized actor_name from auth.uid()→profiles ('system' when unauthenticated), overwriting any caller-supplied value (anti-spoof) — the single actor source for both triggers and app-level events. src/lib/audit.ts logEvent (sign-in/sign-out, delegates the insert to the single repository writer, no-op in local mode, never throws). AuditScreen rewritten: server-filtered (client/actor/action) paged query (50/page, Load more) via useAuditLog + useInfiniteQuery, humanized diff renderer ("Readiness status: Partial → Met"); Local Prototype mode keeps the seed rows behind a "Demo data" badge. audit_read refined (supersedes 004): admins all; assigned BF staff all actions; client-role users their client minus internal-only action types (is_internal_audit_action — one SQL source of truth, enforced server-side); append-only preserved (no update/delete policy). +16 unit tests (auditLog diff renderer/humanizers/mapper) and +6 RLS-suite trigger/policy tests (exact-diff, '[text changed]', no-op→no row, actor stamping, cross-client read=0, internal-action hiding). Gate green (typecheck/build:data/build/143 unit tests/check:supabase-readonly/10 e2e); RLS suite not run locally (no Docker/Supabase CLI) — CI `rls` job is the enforcement point. verifier PASS, security-reviewer PASS WITH NOTES (no exploitable path; actor cannot be spoofed; append-only intact). DEFERRED to Task 11: a client-visible action diff can still carry a short internal field (e.g. consultant_notes) — same row-level/column-level posture as Task 05; no client-role users exist pre-portal, so not exploitable now (documented in 005 + must be closed before any client role goes live). First edit of an un-materialized control logs 'assessment.created' (the row is INSERTed by the repository's materialize-on-write); subsequent edits log '.updated' diffs. NEEDS HUMAN: apply migration 005 to the Supabase project; confirm the `rls` CI job goes green.
- 2026-06-13 — 07 Client CRUD + assignments: the app is now genuinely multi-client. New ClientsRepository (local + Supabase, selected by useRepository().clients) — listClients/getClient/createClient/updateClient/archiveClient + listAssignments/assignConsultant/removeAssignment + listAssessmentStatuses/listAssignableConsultants. New ClientsProvider/useClients()/useCurrentClient() (src/data/clientsStore.tsx) mirrors store.tsx's dual-engine pattern (synchronous localStorage local engine so the route's <ClientScope> validates on first render; TanStack Query supabase engine). DataProvider now takes a route-driven clientId prop (App.tsx CurrentClientScope + src/data/clientRoute.ts clientIdFromPathname); the local engine's assessments are client-scoped (baseAssessmentsFor: demo client = worked seed, every other client = 110 'Not Reviewed'). KILLED CURRENT_CLIENT_ID/CURRENT_CLIENT from all screens + Shell (replaced by useCurrentClient() / useData().currentClientId; DEMO_CLIENT_ID is the seed/default-context constant only) — core/client/controls/work/output screens + Shell. Create Client is a real 5-step wizard (org basics incl. CAGE/DIB role/contract types, CMMC target L1/L2/Undetermined, primary contact, consultant assignment, review) with name-required + near-duplicate warning + email-format validation; on create it inserts the client, optionally assigns a consultant, seeds the 110 assessment rows in ONE batch insert (Supabase) / virtually (local), appends a client.created audit event, and navigates to /clients/:newId. Clients list computes live readiness/SPRS per client from real assessments ('Not started' when no reviewed controls); archiveClient sets status 'Closed' (never hard-deletes); create/archive gated to benchmark_fox_admin (useCanManageClients + RequireRole on /clients/new, pass-through in Local Prototype mode). Migration 006_client_profile_fields.sql (append-only) adds clients profile columns + client_assignments.deleted_at AND redefines is_assigned_to_client/can_write_client to honor deleted_at so removeAssignment actually revokes access. +9 unit tests (createClient seeds exactly 110, archive-not-delete, soft-remove, local+Supabase-mocked) and +2 e2e (wizard create→empty dashboard→edit control→readiness moves; cross-client isolation) and +5 RLS-suite endpoint tests (client create admin-only, cross-client read/write isolation, assignment admin-only, soft-delete revocation). Gate green (typecheck/build:data/build/152 unit tests/check:supabase-readonly/12 e2e +1 pre-existing skip); RLS suite not run locally (no Docker/Supabase CLI) — CI `rls` job is the enforcement point. verifier PASS. KNOWN LIMITATION (pre-existing, out of scope): the internal Dashboard's per-client bar chart still uses static seed numbers (Task 07 targets the Clients list); evidence/POA&M/tasks remain global seed until Tasks 08/10 (assessments/intake/scope are fully per-client + RLS-isolated). NEEDS HUMAN: apply migration 006 to the Supabase project; confirm the `rls` CI job goes green; ensure a Benchmark Fox organization row exists (createClient attaches new clients to the internal org, else first org).
- 2026-06-13 — 08 Evidence lifecycle workflow: the Evidence Hub is now the working request→review workflow (METADATA + external https links only — no file storage, no Supabase Storage, no file column added). src/lib/evidenceWorkflow.ts is the SINGLE source of truth for the legal state machine (EVIDENCE_TRANSITIONS, exactly the 7 spec rules; Missing/Rejected terminal), with assertTransition (typed EvidenceTransitionError on illegal moves), REVIEWER_ONLY_STATUSES (In Review/Accepted/Needs Revision/Rejected — intentionally one wider than the spec: claiming an item into review is a staff act), allowedNextStatuses(from, canReview), and read-time expiry derivation (effectiveStatus/effectiveFreshness — Accepted-past-expiry derives to Expired, no cron). New EvidenceRepository (list/create/updateMetadata/transition; named updateMetadata not `update` to dodge the check:supabase-readonly coarse write scan, like updateClient) — local (bf_evidence_v1; demo seeded from EVIDENCE_ITEMS, other clients empty) + Supabase (writes confined to supabaseRepository.ts; transition validates before writing). Evidence wired into the dual-engine store (useData().evidence + requestEvidence/updateEvidence/transitionEvidence; optimistic in Supabase mode). selectors.ts is the ONE coverage/counts source consumed by all four screens (Evidence Hub, Control Detail, SSP, Reports) + the Client Dashboard — acceptedEvidence/controlEvidenceCoverage/evidenceObjectiveSummary/evidenceCountsByStatus/controlIdsWithoutAcceptedEvidence/evidenceForControl ("covered" = objective referenced by ACCEPTED, expiry-aware evidence). DELETED per-screen duplication: evidenceForControl moved out of data/evidence.ts into selectors; removed direct EVIDENCE_ITEMS imports + inline coverage math from work.tsx/output.tsx/controls.tsx/client.tsx. Evidence Hub rewritten: grouped-by-status table with per-status counts, "controls w/ 0 accepted evidence" filter, Request modal (control → specific objectives or whole control → description → assignee → due date), detail drawer (https-validated secure link + standing "artifact stays in the client's secure store" warning, quality, freshness+expiry, reviewer note, and ONLY the legal next-status buttons, role-gated). Permissions: review transitions are consultant/admin only — hidden in UI (useCanReviewEvidence + allowedNextStatuses) AND enforced server-side by migration 007_evidence_workflow.sql (append-only, auth-guarded like 005): adds objective_ids/description/due_date/expires_on columns (no file column), is_reviewer_only_evidence_status() mirroring the TS set, and a BEFORE INSERT/UPDATE trigger evidence_guard_transition() that blocks non-staff from setting a review-only status (raises 42501) while allowing staff + system/service_role (a trigger because RLS can't compare OLD/NEW status in one predicate). +28 unit tests (evidenceWorkflow exhaustive legality/expiry/role-gating, selectors coverage math, local repo CRUD+transition+isolation, store optimistic transition) and +2 e2e (request→link→review→accept moving coverage + cross-screen agreement on Control Detail; UI role-gating) and +6 RLS-suite tests (uploader may set non-review status, cannot set Accepted/In Review/insert-as-review; staff can Accept and it is audited as evidence.status_changed). Gate green (typecheck/build:data/build/180 unit tests/check:supabase-readonly/14 e2e +1 pre-existing skip); RLS suite not run locally (no Docker/Supabase CLI) — CI `rls` job is the enforcement point. verifier PASS (3 MINOR notes addressed: In Review gating documented as intentional; added RLS tests for the In Review block and audit-row visibility). NEEDS HUMAN: apply migration 007 to the Supabase project; confirm the `rls` CI job goes green. NOTE: in Local Prototype mode evidence transitions are not audited (no local audit trail — Supabase mode logs via the 005 trigger), and the Tasks screen still shows a "[ completion evidence upload ]" wireframe placeholder (pre-existing, out of scope — not a real upload).
- 2026-06-13 — 09 SSP generator (.docx): the SSP Workspace + Reports now produce a real System Security Plan as a Word .docx, generated CLIENT-SIDE and downloaded (no server render, no Supabase Storage, no file persisted — it is the client's document). Authorized new deps added BY NAME only: `docx` + `file-saver` (no @types/file-saver — a 1-line ambient shim src/types/file-saver.d.ts declares just saveAs). src/lib/export/sspModel.ts is the PURE, DOM-free document model (buildSspModel): all 110 controls grouped by the 14 official families in numeric order (compareControlNumbers: 3.1.10 after 3.1.9), each entry carrying the VERBATIM official requirement text (copied through from the library, never paraphrased), readiness status, the authored sspStatement OR a null flag (→ visible red "[IMPLEMENTATION STATEMENT REQUIRED]" placeholder), and related POA&M item ids ONLY when Not Met/Partial; plus System Identification rows (intake+scope), the scope asset inventory, an evidence index (metadata + external-link references, never files), the SPRS estimate (from the untouched scoring engine), and the pre-flight counts. src/lib/export/sspDocx.ts renders that model with docx (buildSspDocxDocument/generateSspBlob/sspFilename): cover page (client, "System Security Plan", system name, CMMC target, version+date, "Prepared with Benchmark Fox", standing disclaimer), auto-updating TOC field (features.updateFields, headingStyleRange 1-2 so 110 control H3s don't bloat it but still sit in Word's nav pane), Montserrat/navy Word heading styles, CONFIDENTIAL footer with page X of Y, tables with repeated header rows, Appendix A (SPRS estimate + DoD AM v1.2.1 citation + reused estimate/readiness disclaimers) and Appendix B (evidence index, links "held in the client's own secure repository"). Shared src/components/SspExport.tsx (GenerateSspButton) wired into both the SSP Workspace and Reports PageHeads: opens a pre-flight summary (X with statements / Y placeholders / Z not reviewed) gated behind "Generate anyway", LAZY-imports the heavy docx renderer (code-split into its own ~364 kB chunk — main bundle unchanged) only on generate, downloads via file-saver, and writes a metadata-only 'report.ssp_generated' audit event (no-op/never-throws in Local mode). New System Name: added IntakeState.systemName (editable intake-summary field) + migration 008_intake_system_name.sql (append-only, additive nullable column, no RLS/policy change) + mappers/database.types round-trip; empty → cover shows a visible placeholder. Disclaimer text extracted verbatim into src/data/disclaimers.ts (READINESS_SUPPORT_DISCLAIMER) and reused by both output.tsx and the SSP (single source, not rewritten). +24 unit tests (sspModel: 110 sections/14 families/numeric order, verbatim text for 3.1.1/3.12.4/3.13.11, placeholder + whitespace-only handling, POA&M refs resolve to real ids, pre-flight + system-name placeholder, evidence index; sspDocx: packs to a non-empty Blob over all 110 sections under 3s, filename slug — document MODEL asserted, not rendered bytes per spec) and +2 e2e (SSP Workspace pre-flight → Generate anyway runs the full in-browser pipeline with no error; Reports exposes the same trigger). Gate green (typecheck/build:data/build/204 unit tests/check:supabase-readonly/16 e2e +1 pre-existing skip). verifier PASS (1 MINOR addressed: hardened the table builder's column-index access; curly-apostrophe + TOC 1-2 range confirmed intentional). NOTE: the e2e asserts the success path (dialog closes, no error) rather than capturing the OS download — file-saver 2.x clicks a detached anchor that headless Chromium accepts but doesn't surface to Playwright; byte-level output is covered by the sspDocx Blob test. The Report Preview screen's separate "Export DOCX/PDF" exec-summary buttons are still placeholders (Task 10 territory, out of scope). NEEDS HUMAN: apply migration 008 to the Supabase project; open a generated .docx in Microsoft Word AND LibreOffice to confirm the TOC populates on open (right-click → Update Field if needed) — the one acceptance step with no automated environment.
- 2026-06-13 — 10 POA&M (.xlsx) + SPRS report (.pdf): the Reports screen now generates two more real client-side deliverables (CLIENT-SIDE generation + download only; no server render, no Supabase Storage, no file persisted), alongside the Task 09 SSP. New shared src/lib/export/common.ts is the ONE home for branding (navy/silver/red/white), the methodology citation (DOD_AM_CITATION), formatLongDate, the re-exported READINESS_SUPPORT_DISCLAIMER, and the unified filename convention exportFilename() → `{clientSlug}_{deliverable}_{YYYY-MM-DD}.{ext}`; sspDocx was refactored to consume it (SSP filename moved from `SSP_{slug}_{date}` to the unified `{slug}_SSP_{date}` — the one sspDocx.test assertion updated to match, intentional). PART A — POA&M .xlsx: src/lib/export/poamModel.ts (PURE model, no exceljs) builds open/closed rows in the DoD/eMASS column layout (Item ID | Control # | Weakness | Severity/Risk | Source | Resources | Scheduled completion [real Date via parsePoamDate] | Milestones [wrapped multi-line ✓/○ label — date] | Milestone changes | Status | Comments) split by openPoamItems (open) vs Complete/Validated/Closed (closed sheet), plus a Score Impact row per open item; src/lib/export/poamXlsx.ts renders it with exceljs (3 sheets POA&M/Closed/Score Impact, navy/white header, frozen row, autofilter, column widths, real date cells, wrapped milestones). The Score Impact projection REUSES the scoring engine via the NEW pure helper projectedScoreIfRemediated() in scoring.ts (current estimate + the control's deductionImpact; NO scoring math reimplemented in the exporter; scoring semantics untouched). PART B — SPRS .pdf: src/lib/export/sprsReportModel.ts (PURE model) reuses the SAME selectors the dashboard/Report Preview use (estimateSprs, readinessPct, statusCounts, scoreByFamily→14 family bars in official order, topFindings, topDeductionDrivers) so PDF numbers can't drift; src/lib/export/sprsReportPdf.tsx renders cover (big SPRS score, readiness %, target) + methodology note (DoD AM v1.2.1 + Partial→Not Met + 3.12.4 NA + verbatim disclaimer) + by-family bar chart drawn with NATIVE @react-pdf Svg/Rect primitives (no html2canvas) + top findings + score-recovery opportunities + a `fixed` footer disclaimer on every page. LIBRARY DECISION: the task authorized "pdfmake OR @react-pdf/renderer". pdfmake was implemented first but its pdfkit getBuffer/getBlob NEVER completes in this app's browser (verified hanging in BOTH the Vite dev server AND a production preview build, headed and headless — it cannot produce a file), so the PDF lib is @react-pdf/renderer (pdfmake fully uninstalled; no leftover refs/shim). exceljs ships its own types and @react-pdf ships its own types, so NO @types/* were added. PART C — wiring: src/components/ReportExports.tsx (GeneratePoamButton + GenerateSprsReportButton, shared pre-flight dialog + useClientExportData hook, heavy renderers LAZY-imported on generate so the main bundle is unchanged — each is its own Vite chunk, exceljs ~942 kB / @react-pdf ~1.47 kB[chunk]; metadata-only audit events report.poam_generated / report.sprs_generated, no-op/never-throws in Local mode) replaces the Reports screen's 8 placeholder cards with three LIVE deliverable cards (SSP/POA&M/SPRS). +34 unit tests (poamModel: open/closed split, column mapping, date parse, milestone formatting, Score Impact == projectedScoreIfRemediated exactly; poamXlsx: 3 named sheets, frozen+autofilter, real Date cell, non-empty Blob, filename; sprsReportModel: numbers == selectors, 14 family bars official order, findings/recovery; sprsReportPdf: element-tree walk asserts cover score/readiness/target, methodology disclaimer+citation+3.12.4, 28 native Rect bars, every family code, footer label "Page X of Y") and +3 e2e (Reports exposes all three triggers; POA&M and SPRS pre-flight → Generate run the full in-browser exceljs/@react-pdf pipeline with no error). Gate green (typecheck/build:data/build/238 unit tests/check:supabase-readonly/19 e2e +1 pre-existing skip). verifier PASS (MINOR stale e2e comments fixed). No migration (POA&M/scoring stay client-side seed). NEEDS HUMAN: open a generated .xlsx in Excel + LibreOffice (header styling, frozen row, autofilter, real dates, wrapped milestones, 3 sheets) and the .pdf in Acrobat + a browser viewer (cover score, native family bars, per-page footer) — the manual acceptance step with no automated environment.
- 2026-06-13 — 11 Client portal (role-scoped) [security]: the four CLIENT roles (client_executive/client_it_owner/evidence_uploader/readonly_viewer) now get a reduced, own-engagement-only portal; RLS (004/006) stays the hard boundary and this is the UX + COLUMN-hiding layer on top. src/auth/roles.ts is the ONE source of role classification (STAFF_ROLES/CLIENT_ROLES) + capabilities (canEditAssessments/canReviewEvidence/canSubmitEvidence/canRequestEvidence/canManageClients) — each takes (role, isConfigured) so a null role fails CLOSED in Supabase mode (signed-in-without-profile = no access) but means the internal staff demo (full access) only in Local Prototype mode; usePermissions() (src/auth/permissions.ts) is the ergonomic hook screens consume. AuthProvider resolves the assigned client ONCE: Supabase mode fetches it from client_assignments via authService.fetchAssignedClientId (deleted_at-aware, RLS-limited to own rows) and folds assignmentPending into loading; Local mode maps a simulated client role → DEMO_CLIENT_ID. A Local-Prototype-ONLY "View as" role switcher (Tweaks → setSimulatedRole, persisted to bf_sim_role; no-op + UI hidden when Supabase configured) drives the demo. PortalGuard (routes.tsx) constrains client roles to their OWN client (any other :clientId → their dashboard) and the reduced PORTAL_SCREEN_KEYS set (any internal route → their dashboard); useGo scopes every nav to assignedClientId so a client never leaves their engagement. Shell renders PORTAL_NAV (Dashboard/Controls/Evidence/Documents/Knowledge — no clients list/audit/settings/internal dashboard/intake/scope/tasks/control-library), a prominent "Client Portal" badge + client name, and hides the internal client-tab strip; App hides the screen launcher + keyboard stepping for portal sessions and the whole Tweaks panel for a REAL client user. COLUMN HIDING (security core): src/data/internalFields.ts is the SINGLE constant for internal-only data (INTERNAL_ONLY_ASSESSMENT_FIELDS=['consultantNotes'] / _COLUMNS=['consultant_notes'], INTERNAL_POAM_CLASS='Internal', stripInternalAssessmentFields, visiblePoamItems) — mirrored by migration 009_client_portal_views.sql which (1) creates client_control_assessments_client WITH (security_invoker=on) so 004 row RLS still scopes rows to the caller while the view OMITS consultant_notes, and (2) supersedes the 005 audit trigger so consultant_notes changes are recorded under a separate <entity>.internal_note action (hidden from client roles by is_internal_audit_action, broadened to '%.internal_note', + audit_read) and STRIPPED from the client-visible diff — closing the 004/005 audit-diff residual. The store threads hideInternal (= isClientRole(role)) into getAssessments so the client-portal read path reads the view (supabase) / strips locally (no seed fall-back leak); evidence/intake/scope have no internal columns. Read-only screens for client roles: control matrix + detail show Status badges instead of editable InlineSelects and hide Consultant Notes + Save/Request/Create actions; Evidence Hub hides "Request" (staff-only), gives evidence_uploader a focused "My queue" (Requested/Needs Revision) + link submission while keeping review staff-only, and makes the drawer fully read-only for executive/it_owner/readonly_viewer; Knowledge hides "New Article"; POA&M/SPRS exports filter internal-class items for client roles. +28 unit tests (roles classification/capabilities/fail-closed, internalFields strip + visiblePoam + lock-step shape, portal.test.tsx nav-filtering + route-guard + read-only matrix per role) and +3 e2e (e2e/portal.spec.ts: reduced shell + internal-route guard + cross-client bounce via the Local-only Tweaks switcher; read-only matrix; evidence_uploader queue) and +5 RLS-suite tests (view omits consultant_notes + row isolation through the invoker view, column-select errors, staff base-table read unchanged, *.internal_note hidden from client roles but visible to staff; the Task 06 long-note test updated for the new internal-note routing). Gate green (typecheck/build:data/build/263 unit tests/check:supabase-readonly/22 e2e +1 pre-existing skip); RLS suite not run locally (no Docker/Supabase CLI) — CI `rls` job is the enforcement point. verifier PASS, security-reviewer PASS WITH NOTES. KNOWN/ACCEPTED RESIDUAL (documented in 009 + tracked): with Supabase's single `authenticated` Postgres role, column privileges can't be per-app-role, so a logged-in client user could still read consultant_notes for their OWN client by querying the base table directly with the anon key (cross-client reads stay RLS-blocked — the catastrophic case is closed). consultant_notes is internal Benchmark Fox commentary, NEVER CUI (CUI banned product-wide), the portal app never reads that column, and the structural fix (move internal notes to a staff-only table / per-role DB roles) is the documented follow-up. NEEDS HUMAN: apply migration 009 to the Supabase project; confirm the `rls` CI job goes green; provision real client-role users with exactly one client_assignments row each (the portal scopes them to that engagement).
- 2026-06-12 — 04 Repository layer [security]: assessments/intake/scope moved behind a ClientDataRepository (src/data/repository/) consumed through @tanstack/react-query. Dual engine in store.tsx — local engine is the verbatim localStorage logic (store.test.tsx unchanged, bit-identical), supabase engine does useQuery reads + optimistic patch/save mutations (rollback + dismissible toast, isMutating-guarded invalidate), loading gate + Retry. supabaseRepository materializes assessment rows on first write (upsert onConflict client_id,control_id), latest-non-deleted-row for intake/scope, asset reconcile via soft-delete (never hard delete); all errors → RepositoryError with safe messages. Migration 003 adds ssp_statement (append-only). One-time MigrationPrompt imports local edits on explicit action only. Demo client bridged via fixed uuid + db:seed:demo. check:supabase-readonly tightened (writes only in supabaseRepository.ts, hard delete banned everywhere). DataProvider moved below AuthProvider. +49 unit tests (mappers/selection/supabaseRepository/store-supabase/migration-prompt), e2e 10 pass +1 skip; verifier PASS, security-reviewer PASS (no exploitable findings; uuid-passthrough + RLS noted for Task 05/07). NEEDS HUMAN: apply migration 003 + run db:seed:refs and db:seed:demo before live testing; RLS workflow policies don't exist until Task 05 so live cloud reads/writes stay empty/blocked until then.
