# Build Queue — Benchmark Fox Readiness Portal

Rules: strictly in order. One task per Claude Code session (/clear between).
Merge each task's branch before starting the next. [security] tasks get the
security-reviewer subagent in addition to verifier.

- [x] 01 — Testing infrastructure (Vitest + Playwright + CI) — prompts/01-testing-infrastructure.md
- [x] 02 — React Router, client-scoped URLs — prompts/02-react-router.md
- [x] 03 — Supabase Auth [security] — prompts/03-supabase-auth.md
- [x] 04 — Repository layer: localStorage → Supabase [security] — prompts/04-repository-layer.md
- [x] 05 — RLS hardening + automated policy tests [security] — prompts/05-rls-hardening.md
- [x] 06 — Real audit logging — prompts/06-audit-logging.md
- [ ] 07 — Client CRUD + assignments — prompts/07-client-crud.md
- [ ] 08 — Evidence lifecycle workflow — prompts/08-evidence-workflow.md
- [ ] 09 — SSP generator (.docx) — prompts/09-ssp-docx.md
- [ ] 10 — POA&M (.xlsx) + SPRS report (.pdf) — prompts/10-poam-xlsx-sprs-pdf.md
- [ ] 11 — Client portal (role-scoped) [security] — prompts/11-client-portal.md
- [ ] 12 — Resilience pass (errors, Sentry, a11y) — prompts/12-resilience-pass.md
- [ ] 13 — CI/CD hardening + security headers — prompts/13-cicd-hardening.md

## Completed log
(/build-next appends here: date, task, one-line summary)

- 2026-06-12 — 01 Testing infrastructure: Vitest (63 unit tests: scoring/selectors/objectives/store/integrity, src/lib at 100% line coverage for tested modules) + Playwright chromium smoke suite (5 tests) + CI workflow; verifier PASS.
- 2026-06-12 — 02 React Router: replaced the custom ?screen= router with react-router-dom@6 — client-scoped URLs for all 21 screens (src/routes.tsx: route tree, ScreenKey↔path map, legacy ?screen= redirect, ProtectedRoute placeholder, NotFound); keyboard/launcher/tweaks UX preserved; +9 unit and +3 e2e tests; verifier PASS.
- 2026-06-12 — 03 Supabase Auth: migration 002 (profiles.role default readonly_viewer, invite-safe handle_new_user/handle_user_email_confirmed triggers, self-scoped SELECT-only RLS policy) + authService/AuthProvider (generic errors, magic-link anti-enumeration), real Login (password + magic link, return-to-destination), ProtectedRoute/RequireRole, Shell user menu; Local Prototype mode keeps working behind a dismissible banner; +7 unit and +2 e2e tests (1 real-auth stub skipped); verifier PASS, security-reviewer PASS (after invite-capture hardening). NOTE for Supabase project setup: disable public email signups (invite-only portal).
- 2026-06-12 — 05 RLS hardening [security]: migration 004_rls_policies.sql turns the draft into the applied tenancy boundary — 6 SECURITY DEFINER helpers (repointed from the empty user_roles table to the authoritative profiles.role; STABLE, search_path=public, execute granted to authenticated only) + policies on all 18 RLS-enabled tables with a deny-all checklist. admin r/w all; consultant r/w only assigned clients; client_executive/it_owner/readonly_viewer read-only on their one assigned client; evidence_uploader reads assigned client + insert/update evidence metadata only (cannot write assessments); reference tables read-only via auth keys (service_role-only writes); audit_events append-only (insert on accessible clients, no update/delete). Every write carries WITH CHECK; no tenant policy uses auth.uid() directly. Kept 002's no-self-update on profiles (added admin-read only) — no self role-escalation. Automated suite scripts/test-rls.mjs (node:test + @supabase/supabase-js, no new deps) proves cross-client reads return zero rows, writes are blocked, reference is read-only, audit is immutable; new CI `rls` job (supabase/setup-cli + supabase start) enforces it. Gate green (typecheck/build:data/build/127 unit tests/check:supabase-readonly); RLS suite not run locally (no Docker/Supabase CLI on dev machine) — CI is the enforcement point. verifier PASS, security-reviewer PASS (no exploitable cross-client path; column-level hiding of internal notes from client-role users deferred to Task 11 portal — row-level isolation is complete and no client-role users exist pre-portal). NEEDS HUMAN: apply migration 004 to the Supabase project; confirm the `rls` CI job goes green on first push (validates config.toml against the CLI version).
- 2026-06-12 — 06 Real audit logging: migration 005_audit_triggers.sql makes the Audit Log real — AFTER INSERT/UPDATE triggers on assessments/intake/scope/evidence write a compact {field:{old,new}} jsonb diff into audit_events.new_value (changed columns only; id/timestamps/FK columns excluded; free-text >500 chars collapsed to '[text changed]' so no CUI-adjacent text enters the trail; empty diffs skipped → exactly one row per meaningful edit; actions <entity>.created/.updated, intake/scope .saved, evidence.status_changed). A BEFORE INSERT trigger (audit_set_actor) stamps user_id + the new denormalized actor_name from auth.uid()→profiles ('system' when unauthenticated), overwriting any caller-supplied value (anti-spoof) — the single actor source for both triggers and app-level events. src/lib/audit.ts logEvent (sign-in/sign-out, delegates the insert to the single repository writer, no-op in local mode, never throws). AuditScreen rewritten: server-filtered (client/actor/action) paged query (50/page, Load more) via useAuditLog + useInfiniteQuery, humanized diff renderer ("Readiness status: Partial → Met"); Local Prototype mode keeps the seed rows behind a "Demo data" badge. audit_read refined (supersedes 004): admins all; assigned BF staff all actions; client-role users their client minus internal-only action types (is_internal_audit_action — one SQL source of truth, enforced server-side); append-only preserved (no update/delete policy). +16 unit tests (auditLog diff renderer/humanizers/mapper) and +6 RLS-suite trigger/policy tests (exact-diff, '[text changed]', no-op→no row, actor stamping, cross-client read=0, internal-action hiding). Gate green (typecheck/build:data/build/143 unit tests/check:supabase-readonly/10 e2e); RLS suite not run locally (no Docker/Supabase CLI) — CI `rls` job is the enforcement point. verifier PASS, security-reviewer PASS WITH NOTES (no exploitable path; actor cannot be spoofed; append-only intact). DEFERRED to Task 11: a client-visible action diff can still carry a short internal field (e.g. consultant_notes) — same row-level/column-level posture as Task 05; no client-role users exist pre-portal, so not exploitable now (documented in 005 + must be closed before any client role goes live). First edit of an un-materialized control logs 'assessment.created' (the row is INSERTed by the repository's materialize-on-write); subsequent edits log '.updated' diffs. NEEDS HUMAN: apply migration 005 to the Supabase project; confirm the `rls` CI job goes green.
- 2026-06-12 — 04 Repository layer [security]: assessments/intake/scope moved behind a ClientDataRepository (src/data/repository/) consumed through @tanstack/react-query. Dual engine in store.tsx — local engine is the verbatim localStorage logic (store.test.tsx unchanged, bit-identical), supabase engine does useQuery reads + optimistic patch/save mutations (rollback + dismissible toast, isMutating-guarded invalidate), loading gate + Retry. supabaseRepository materializes assessment rows on first write (upsert onConflict client_id,control_id), latest-non-deleted-row for intake/scope, asset reconcile via soft-delete (never hard delete); all errors → RepositoryError with safe messages. Migration 003 adds ssp_statement (append-only). One-time MigrationPrompt imports local edits on explicit action only. Demo client bridged via fixed uuid + db:seed:demo. check:supabase-readonly tightened (writes only in supabaseRepository.ts, hard delete banned everywhere). DataProvider moved below AuthProvider. +49 unit tests (mappers/selection/supabaseRepository/store-supabase/migration-prompt), e2e 10 pass +1 skip; verifier PASS, security-reviewer PASS (no exploitable findings; uuid-passthrough + RLS noted for Task 05/07). NEEDS HUMAN: apply migration 003 + run db:seed:refs and db:seed:demo before live testing; RLS workflow policies don't exist until Task 05 so live cloud reads/writes stay empty/blocked until then.
