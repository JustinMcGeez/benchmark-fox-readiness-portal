# Project npm defaults. Intentionally does NOT disable TLS verification.
# If you are behind a TLS-intercepting proxy and `npm install` fails with
# UNABLE_TO_VERIFY_LEAF_SIGNATURE, configure it LOCALLY (not in the repo) —
# see the "Network / proxy" section in README.md.
fund=false
audit=false
